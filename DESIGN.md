# 设计方向

本文档记录无法仅从实现中推断出的视觉意图与设计边界。所有视觉数值、组件状态、响应式行为和动效均以 `css/styles.css` 为唯一事实来源。

## 视觉定位

本站是一个安静的个人学术档案。整体结合温暖的纸张质感、编辑式排版、清晰的时间顺序、纤细的档案分隔线，以及少量具有触感的研究标记。页面应体现个人表达与学术气质，而非宣传感。

## 核心原则

- 将页面视为一张连续的纸面，通过字体、间距、时间顺序和细线建立层级，而不是依赖容器。
- 使用衬线字体承载学术叙述和论文细节；使用无衬线字体呈现导航、标签和紧凑的元数据。
- 论文与时间线内容应保持平面、清晰、易读，并具有文献档案感。
- Venue 与 Award 强调色仅用于表达来源或荣誉。
- Sticker 与 Halftone 素材应作为克制的材质细节，服务于内容而不主导构图。
- 动效只用于阐明交互或元素关系，不得造成布局位移，也不能让减少动态效果的用户失去可见状态反馈。

## 扩展规则

- 创建或修改界面前先检查 `css/styles.css`，新增基础样式前优先复用其中的 token 和语义类族。
- 字体使用 `.type-*`，页面导轨和节奏使用 `.section-*`，时间顺序条目使用 `.editorial-*`；组件优先沿用 `.site-*`、`.profile-*`、`.publication-*` 和 `.news-*` 等现有命名空间。
- 只有真正共享的数值或具有明确语义的角色才应成为 token；单一组件的视觉微调应保留在所属组件中。
- 页面特例必须使用具有语义的类名；不得使用内联样式，也不得通过 JavaScript 生成颜色或间距声明。
- 在窄屏上保持正确的阅读顺序，并为交互元素提供清晰的焦点状态。任何新动效都必须提供合理的 reduced-motion 结果。
- 新增 Venue 视觉身份时，同时更新 `js/utils.js` 中的注册表，以及对应的 CSS token 和语义类。

## 避免采用

- SaaS 落地页式 Hero、营销功能网格或通用作品集模板。
- 厚重或嵌套的卡片、玻璃拟态、模糊渐变光球、环境光晕及大面积装饰色块。
- 在语义角色之外将 Venue、Award 或焦点色作为装饰使用。
- 在编辑式列表页面使用过大的海报图像，或加入会与论文内容争夺注意力的效果。
- 将圆角矩形、阴影或动画变成默认视觉语言。

## 事实来源

- `css/styles.css`：视觉实现与可复用的设计 API。
- 根目录 HTML 文件：页面结构与语义组合。
- `js/renderers.js`：生成式组件标记。
- `docs/content-schema.md`：内容编辑约定。
- `docs/sticker-logo-recipe.md`：制作 Sticker 素材时按需查阅的说明。

当设计发生演进时，应先修改 CSS；只有底层视觉意图或设计边界改变时，才同步更新本文档。不要在这里复制 CSS 数值。
